# ML Artifacts

Store preprocessing assets here, grouped by purpose:

- `preprocessing/`: reusable transformation pipelines
- `scalers/`: fitted scalers
- `encoders/`: fitted encoders and categorical mappings
- `explainability/`: SHAP explainers, background samples and contribution maps

These files must match the training pipeline exactly.
